# ====================================
# AI Provider Settings
# ====================================

# Provider selection flag
# 0 = OpenAI, 1 = Gemini, -1 = Auto (default)
PROVIDER_FLAG=-1

# ====================================
# OpenAI Settings
# ====================================
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini

# ====================================
# Google Cloud / Gemini Settings
# ====================================
GOOGLE_CLOUD_PROJECT=your_gcp_project_id_here
GCP_PROJECT_ID=your_gcp_project_id_here
GEMINI_MODEL=gemini-1.5-pro

# ====================================
# Twitter/X API Settings (for posting)
# ====================================
X_API_KEY=your_twitter_api_key_here
X_API_SECRET=your_twitter_api_secret_here
X_ACCESS_TOKEN=your_twitter_access_token_here
X_ACCESS_TOKEN_SECRET=your_twitter_access_token_secret_here

# ====================================
# Twitter/X API v2 Settings (for feedback collection)
# ====================================

# Your Twitter User ID (Required when using Bearer Token)
# Get your user ID from: https://tweeterid.com/ or your Twitter profile
# If using OAuth 1.0a credentials below, this is optional (auto-detected)
X_USER_ID=your_twitter_user_id_here

# Option 1: Bearer Token (App-only authentication)
# Get this from Twitter Developer Portal > Your App > Keys and tokens
# Note: When using Bearer Token, X_USER_ID above is REQUIRED
X_BEARER_TOKEN=your_bearer_token_here

# Option 2: OAuth 1.0a credentials (User-context authentication)
# These are the same as the posting credentials above
# When using OAuth 1.0a, X_USER_ID is optional (auto-detected via API)
# The FeedbackCollector will use X_API_KEY, X_API_SECRET, X_ACCESS_TOKEN, X_ACCESS_TOKEN_SECRET
# if X_BEARER_TOKEN is not provided
